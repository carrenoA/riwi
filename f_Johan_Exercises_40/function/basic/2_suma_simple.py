#Crea una función sumar(a, b) que retorne la suma de dos números.

def sum(a,b):
    return a+b
#we use return bc we'll use the function in the code, it's not necessary return it if we use it to print

print(sum(2,1))