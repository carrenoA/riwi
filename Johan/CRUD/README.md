📚 Student CRUD

This project is a simple console-based CRUD application (Create, Read, Update, Delete) for managing student records.
All data is stored in memory using a list of dictionaries.

------------------------------------------------------------------------------------

🚀 Features

*✔ Create a Student*

Allows the user to input a student's:

- Name
- Age
- Phone number
- Subject

Each new record is appended to the students list.

*✔ Read Students*

Displays all students with their details in a clean, structured format.

*✔ Update Student Information*

Updates a specific attribute of a student by searching for their name.

*✔ Delete a Student*

Removes a student from the list by matching their phone number.

*✔ Calculate Average Age*

Iterates over all students and calculates the average of their ages.

*✔ Show Menu*

The program includes a menu that lets the user navigate through all operations.
It also includes basic validation and clean exit using sys.exit().

------------------------------------------------------------------------------------

🧠 Technologies Used

* time module → used for console delays (time.sleep())
* sys module → used to exit the program (sys.exit())

------------------------------------------------------------------------------------

▶️ How to Run

* Python installed.
* Run the program from the terminal with this command:
*python main.py*

* Use the menu to interact with the system.